services:
  - type: web
    name: amjstar-frontend
    env: static
    branch: stagging
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
